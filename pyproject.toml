[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "myapp"
version = "0.0.0"
description = "Minimal sample Python project created by assistant"
authors = [ { name = "you" } ]
readme = "README.md"
requires-python = ">=3.8"
